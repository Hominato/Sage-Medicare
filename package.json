{
  "name": "sage-medicare-monorepo",
  "scripts": {
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "cd backend && prisma generate && npm run build && cd ../frontend && npm run build"
  }
}
